import React from 'react'

const MangaCard = ({manga}) => {
  return (
    <div>
        {manga.title}
        {manga.summary}
        {manga.title}
        {manga.title}
    </div>
    
  )
}

export default MangaCard